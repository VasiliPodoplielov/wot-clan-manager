import { FormErrorsText } from './form-errors-text';

describe('FormErrorsText', () => {
  it('should create an instance', () => {
    const directive = new FormErrorsText();
    expect(directive).toBeTruthy();
  });
});
